server:
  port: 80

spring:
  datasource:
    password: 123456
    username: root
    url: jdbc:mysql://localhost:3306/springboot?characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  type-aliases-package: com.ivan.tkmybatis.bean
  mapper-locations: classpath:mapper/*.xml # 自定义mapper,解决一些tk不能解决的操作
